@attribute [ComponentDoc(
    Slug = "icon",
    Name = "Icon",
    Group = "Conteúdo",
    Description = "Ícones vetoriais da fonte Bootstrap Icons.",
    Code = "<Icon Name=\"check-circle\" SizeClass=\"fs-3\" ClassExtra=\"text-success\" Label=\"Concluído\" />",
    Guidance = "Informe Name sem o prefixo bi-. Sem Label, o ícone é decorativo e fica oculto de leitores de tela. A fonte Bootstrap Icons deve estar carregada."
)]

<i @attributes="AdditionalAttributes" class="@ComponentAttributes.Classes(AdditionalAttributes, "bi", $"bi-{Name}", SizeClass, ClassExtra)" style="@(Style ?? ComponentAttributes.Get(AdditionalAttributes, "style"))" role="@(string.IsNullOrWhiteSpace(Label) ? null : "img")" aria-label="@Label" aria-hidden="@(string.IsNullOrWhiteSpace(Label) ? "true" : null)"></i>

@code {
    [ParameterDoc("Descrição acessível do ícone. Sem Label, o ícone é decorativo.")]
    [Parameter] public string? Label { get; set; }
    /// <summary>
    /// Nome do ícone do Bootstrap (ex: "check-lg", "trash", "search", "person").
    /// </summary>
    [ParameterDoc("Nome do Bootstrap Icon.")]
    [Parameter, EditorRequired]
    public string Name { get; set; } = string.Empty;

    /// <summary>
    /// Classe de tamanho do Bootstrap para tipografia (ex: "fs-1", "fs-2", "fs-5").
    /// </summary>
    [ParameterDoc("Utilitário de tamanho, como fs-3.")]
    [Parameter]
    public string? SizeClass { get; set; }

    /// <summary>
    /// Classes CSS adicionais.
    /// </summary>
    [ParameterDoc("Classes CSS adicionais. Nos campos, estiliza o contêiner.")]
    [Parameter]
    public string? ClassExtra { get; set; }

    /// <summary>
    /// Estilos inline adicionais.
    /// </summary>
    [ParameterDoc("Estilos CSS inline do ícone.")]
    [Parameter]
    public string? Style { get; set; }

    /// <summary>
    /// Captura outros atributos HTML passados ao componente.
    /// </summary>
    [ParameterDoc("Atributos HTML adicionais; class é combinada às classes do componente.")]
    [Parameter(CaptureUnmatchedValues = true)]
    public Dictionary<string, object>? AdditionalAttributes { get; set; }
}


